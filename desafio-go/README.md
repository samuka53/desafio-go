### Exemplo Completo do README:

```markdown
# Projeto FullCycle

Este é o projeto FullCycle que pode ser executado com Docker. Para rodar a imagem, use o seguinte comando:

```bash
docker run samukavno/fullcycle:latest